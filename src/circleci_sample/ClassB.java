package circleci_sample;

public class ClassB {

    @Override
    public boolean equals(Object o) {
        if (o == null) {
            return false;
        }

        return super.equals(o);
    }
}
